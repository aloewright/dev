.wrangler/
dist/
node_modules/
.env
.env.*
npm-debug.log*

# Local Codex/OMX state
.omx/
.ruflo/
verification/

# local scratch (claude)
.probes.*
*.scratch

# secrets / generated (never commit)
.dev.vars
.dev.vars.*
infra/hindsight-monitor/worker-configuration.d.ts

# this repo uses bun (bun.lock is authoritative; Workers Builds runs `bun install --frozen-lockfile`).
# npm's lockfile drifts from bun.lock and is used by no CI — keep it untracked.
package-lock.json
